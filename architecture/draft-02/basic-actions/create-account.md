# Action Creator - Create Account (final)
This action creates signer's subaccount.
Here and further we will follow the Near Core naming for actions.

```ts
// This action takes no args;
type Args = never;

createAccount();
```
